export const enum Locale {
  ZH = 'zh',
  EN = 'en',
}

export const enum OnlineState {
  onLine = 1,
  OffLine = 0,
}

export enum Gender {
  Male = 'male',
  Female = 'female',
  NotSet = 'notSet',
}